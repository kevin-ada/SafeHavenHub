{
  "dependencies": {
    "flowbite": "^2.3.0",
    "tailwindcss": "^3.4.1"
  },
  "engines": {
    "node": "16.x",
    "npm": "10.2.4"
  },
  "scripts": {
    "build:digitalocean": "npm install --production=false && npm run build && npm ci"
  }
}